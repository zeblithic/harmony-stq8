[package]
name = "stq8-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Speech-to-Q8 phoneme classifier core library"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
